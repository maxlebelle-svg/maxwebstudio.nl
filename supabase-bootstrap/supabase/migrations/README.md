# Persistent baseline root

This directory permanently contains exactly the authoritative baseline materialization. Historical migrations are forbidden. Approved post-cutover common migrations may be materialized only from the canonical common source after byte-identity validation; none are approved in F0-f.
